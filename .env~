# Sentry monitoring (optional)
VITE_SENTRY_DSN=
VITE_SENTRY_ORG=
VITE_SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# Database configuration (pick ONE method)
# Option 1: Full connection URL (takes precedence)
DATABASE_URL=postgresql://hikma:hikma_secret@127.0.0.1:5432/hikma_dev
DB_SSL=false
# Option 2: Azure connection string
AZURE_POSTGRESQL_CONNECTIONSTRING=
# Option 3: Individual vars (used when neither URL is set)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=hikma_dev
# DB_USER=postgres
# DB_PASSWORD=postgres

# SSL configuration
# DB_SSL=false
# Set to true when using properly signed certificates (e.g., AWS RDS, Azure).
# Render.com uses self-signed certs, so keep false for Render deployments.
# DB_SSL_REJECT_UNAUTHORIZED=false

# Application
APP_ENV=dev_local
# MAX_HISTORY_DAYS_SYNC=120

# Test credentials (development only)
VITE_ADMIN_EMAIL=admin@example.com
VITE_ADMIN_PASS=changeme

# Test database (for integration tests)
TEST_DATABASE_URL=postgresql://user:password@localhost:5432/hikma_test

# Storage (optional)
# PHOTOS_STORAGE_BUCKET=
# EXPORTS_STORAGE_BUCKET=dev-api-exports
# LOCAL_PHOTO_STORAGE_DIR=/tmp/hikma_photos

# Server
# SERVER_URL=http://localhost:3000
# VITE_APP_TITLE=Hikma Health EHR
